#include "Window.h"
GC pen;
int main(int argc, char* argv[]){
	Window win,child;
	win = Create_Window(10,10,300,300);
	Create_Pen(win,pen);	
	SetWindowTitle(win,"Fereastra");
	ShowWindow(win);
	CloseWindow();
	return 0;
}
